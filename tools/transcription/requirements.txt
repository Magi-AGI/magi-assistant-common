# Offline session-transcript recovery pipeline (WhisperX + pyannote).
# Tested on Windows + RTX 4090, CUDA 12.8, Python 3.12.
#
# Install torch/torchaudio from the CUDA 12.8 wheel index FIRST, then the rest:
#   pip install torch==2.8.0 torchaudio==2.8.0 --index-url https://download.pytorch.org/whl/cu128
#   pip install -r requirements.txt
#
# pyannote diarization is license-gated: accept the model terms on Hugging Face and
# export HF_TOKEN before running.

whisperx==3.8.6
faster-whisper==1.2.1
ctranslate2==4.7.2
pyannote.audio==4.0.4
transformers==4.57.6
onnxruntime==1.26.0
numpy==2.4.4

# torch==2.8.0+cu128        # install via the cu128 index URL above, not from PyPI
# torchaudio==2.8.0+cu128   # idem
